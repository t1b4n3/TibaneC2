# Frontend for web panel

```
/tasks/ - > show all tasks
/tasks?task_id=[x] -> show tasks single task (update and delete options)
/implants/ -> show all implants
/implants?impland_id=[xxx] -> show data for only specific implant
/
```